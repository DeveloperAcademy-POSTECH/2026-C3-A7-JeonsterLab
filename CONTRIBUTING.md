# 작업 컨벤션

## 커밋

`[태그] 한글 제목` 형식으로 작성합니다. 태그는 소문자, 제목은 50자 이내의
간단한 명령조로 작성합니다. 추가 설명은 본문에 작성합니다.

- `init`: 최초 커밋
- `feat`: 새로운 기능 구현
- `fix`: 버그나 오류 해결
- `docs`: README, 템플릿 등 문서 수정
- `setting`: 프로젝트 설정 변경
- `add`: 사진 등 에셋이나 라이브러리 추가
- `refactor`: 기존 코드 리팩토링 또는 수정
- `chore`: 사소한 수정

예: `[feat] 로그인 기능 구현`

기능·수정 범위별로 커밋을 나눕니다. 이미 원격에 공유된 커밋은 합의 없이
재작성하지 않습니다.

## 브랜치

작업 브랜치 형식: `태그/#이슈번호-작업하는 파일`

예: `feat/#1-loginUI`

- `main`: 출시 브랜치
- `develop`: 기본 개발 통합 및 최종 확인 브랜치
- 작업 브랜치: 기능 개발과 버그 수정은 개별 작업 브랜치에서 진행

작업을 마치면 작업 브랜치에 `develop`을 먼저 머지하고, `develop`을 대상으로
Pull Request를 요청합니다.

현재 v1.0 출시 작업은 별도 지정된 `final/release-v1.0` 브랜치를 유지합니다.
이 문서만으로 원격 기본 브랜치나 기존 브랜치 이름을 변경하지 않습니다.
